# Core trading robot
websockets>=12.0
pandas>=2.0
numpy>=1.26
redis>=5.0

# ML/DL stack (optional — comment out if not needed)
xgboost>=2.0
scikit-learn>=1.4
torch>=2.2        # PyTorch for LSTM

# API server
fastapi>=0.110
uvicorn[standard]>=0.27
python-dotenv>=1.0

# LLM / RAG (optional)
openai>=1.20       # or any OpenAI-compatible client

# Utilities
httpx>=0.27
python-jose[cryptography]>=3.3   # JWT for API

# DB persistence (SQLite + Alembic migrations)
sqlalchemy>=2.0
alembic>=1.13
