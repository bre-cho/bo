# FastAPI backend URL (used by Next.js rewrites in development)
NEXT_PUBLIC_API_URL=http://localhost:8000

# API key for mutating control endpoints (must match API_SECRET_KEY in .env)
NEXT_PUBLIC_API_KEY=change_me
